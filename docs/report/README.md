# Final report

This directory contains the updated resubmission report:

- `Relazione_Agenda_Qt_Acampora.docx`: editable source;
- `Relazione_Agenda_Qt_Acampora.pdf`: final submission document.

The report was reviewed against issue #52 and the mandatory requirements audit. It contains:

- student name and number;
- originality declaration;
- introduction and model description;
- updated UML diagrams;
- explicit analysis of Visitor and Command polymorphism;
- explanation of the remaining justified `ActivityKind` uses;
- JSON persistence architecture;
- in-window creation and editing workflows;
- implemented features and automated validation;
- updated hour accounting;
- the mandatory `Modifiche rispetto alla consegna precedente` section;
- build instructions and conclusion.

Validation performed during authoring:

- A4 layout;
- 10 pt body text;
- 8 pages;
- visual render review of every page;
- no clipped text, overlapping elements, or broken figures.
